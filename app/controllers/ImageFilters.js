const ImageFilters = require("../models/ImageFilters");

// Create and Save a new License Agreement
exports.create = (req, res) => {
  if (!req.body) {
    res.json({
      message: "Content can not be empty!",
      status: false,
     });
  }  
  ImageFilters.create( req, res);
};
exports.viewAll = (req, res) => {
  if (!req.body) {
    res.json({
      message: "Content can not be empty!",
      status: false,
     });
  }  
  ImageFilters.viewAll( req, res);
};
exports.viewSpecific = (req, res) => {
  if (!req.body) {
    res.json({
      message: "Content can not be empty!",
      status: false,
     });
  }  
  ImageFilters.viewSpecific( req, res);
};

exports.viewEnable = (req, res) => {
  if (!req.body) {
    res.json({
      message: "Content can not be empty!",
      status: false,
     });
  }  
  ImageFilters.viewEnable( req, res);
};
exports.Enable_DisableFilter = (req, res) => {
  if (!req.body) {
    res.json({
      message: "Content can not be empty!",
      status: false,
     });
  }  
  ImageFilters.Enable_DisableFilter( req, res);
};

exports.viewDisable = (req, res) => {
  if (!req.body) {
    res.json({
      message: "Content can not be empty!",
      status: false,
     });
  }  
  ImageFilters.viewDisable( req, res);
};


exports.delete = (req, res) => {
  if (!req.body) {
    res.json({
      message: "Content can not be empty!",
      status: false,
     });
  }  
  ImageFilters.delete( req, res);
};
exports.update = (req, res) => {
  if (!req.body) {
    res.json({
      message: "Content can not be empty!",
      status: false,
     });
  }  
  ImageFilters.update( req, res);
};
